OCI (Docker) containers for various various use, available "ocis" here:

- cgc: ngc tf container + tf ecosystem + jax + netcdf4

- dkdf: ngc tf container + audio-related software


To pull <oci> from above:

- docker pull ngam00/<oci>

- singularity pull docker://ngam00/<oci>
