OCI (Docker) containers for various uses, available "ocis" here:
- cgc: ngc tf container + tf ecosystem + jax + netcdf4
- dkdf: ngc tf container + audio-related software

To pull <oci> from above:
- docker pull ngam00/<oci> or docker pull ghcr.io/ngam/<oci>
- singularity pull docker://ngam00/<oci> or singularity pull docker://ghcr.io/ngam/<oci>
