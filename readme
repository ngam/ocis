OCI (Docker) containers for various uses, available "ocis" here:

- cgc: ngc container + netcdf4
- dkdf: ngc container + audio-related software

Frameworks, "fw":

- pt: pytorch
- tf: tensorflow

To pull <oci> from above:

- docker pull ngam00/<oci>:<fw> or docker pull ghcr.io/ngam/<oci>:<fw>
- singularity pull docker://ngam00/<oci>:<fw> or singularity pull docker://ghcr.io/ngam/<oci>:<fw>
