name: cgc

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'cgc/*'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file cgc/Dockerfile

  # if_merged: later